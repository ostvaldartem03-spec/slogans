# 🌐 Web Deployment - Cannes Slogan Generator

## ✅ Статус

Flask веб-интерфейс создан и запущен локально!

## 📱 Веб-интерфейс

### Локальный доступ:
```
http://localhost:5000
```

### Здоровье сервера:
```
http://localhost:5000/health
```

## 🎨 Возможности веб-интерфейса

### Главная страница (/)
- 📊 Статистика проекта (15K корпус, 2500 кандидатов, 400 финал)
- ✨ Список возможностей (6 feature-карточек)
- ⚙️ 10-ступенчатый пайплайн
- 💡 Примеры слоганов (RU + EN)
- 🔗 Ссылки на GitHub

### API Endpoints

#### GET /api/info
Информация о проекте
```json
{
  "name": "Cannes Slogan Generator",
  "version": "1.0.0",
  "features": [...]
}
```

#### GET /api/config
Текущая конфигурация пайплайна

#### GET /api/examples
Примеры слоганов на русском и английском

#### GET /api/stats
Статистика выполнения (если есть результаты)

#### GET /api/download/<filename>
Скачать результаты (cannes_400.jsonl/csv/txt)

#### GET /health
Health check endpoint

## 🚀 Запуск локально

### Вариант 1: Напрямую
```bash
cd /project/workspace/slogans
python web/app.py
```

### Вариант 2: С настройками
```bash
export PORT=8080
export FLASK_ENV=development
python web/app.py
```

## 🌍 Cloudflare Tunnel

### Автоматический запуск:
```bash
# Установить cloudflared (уже сделано)
/tmp/cloudflared tunnel --url http://localhost:5000
```

Туннель создаёт временный публичный URL вида:
```
https://[random].trycloudflare.com
```

### Процессы
Сейчас запущено:
- ✅ Flask server на порту 5000
- ✅ Cloudflared tunnel (процесс активен)

### Получить URL туннеля:
```bash
# Проверьте логи
cat /tmp/tunnel-output.txt
# или
cat /tmp/cloudflare-tunnel.log
```

URL должен содержать: `https://xxxxx.trycloudflare.com`

## 📁 Структура веб-приложения

```
web/
├── app.py              # Flask приложение (200 строк)
└── templates/
    └── index.html      # Главная страница (400 строк HTML+CSS+JS)
```

## 🎨 Дизайн

- Градиентный фиолетовый фон
- Адаптивная сетка (grid layout)
- Анимированные карточки
- Асинхронная загрузка примеров
- Responsive дизайн (mobile-friendly)

## 🔧 Технологии

- **Backend**: Flask (Python)
- **Frontend**: Vanilla JS, CSS3, HTML5
- **Tunnel**: Cloudflare Argo Tunnel
- **API**: REST JSON

## 📊 Endpoints Summary

| Endpoint | Method | Описание |
|----------|--------|----------|
| `/` | GET | Главная страница |
| `/health` | GET | Health check |
| `/api/info` | GET | Информация о проекте |
| `/api/config` | GET | Конфигурация |
| `/api/examples` | GET | Примеры слоганов |
| `/api/stats` | GET | Статистика |
| `/api/docs` | GET | README документация |
| `/api/download/<file>` | GET | Скачать результаты |

## 🐛 Отладка

### Проверить статус Flask:
```bash
curl http://localhost:5000/health
```

### Проверить cloudflared:
```bash
ps aux | grep cloudflared
```

### Просмотреть логи:
```bash
# Flask
cat /tmp/flask.log

# Cloudflare
cat /tmp/tunnel-output.txt
```

## 🎯 След Я шаги

1. ✅ Flask приложение создано
2. ✅ Flask сервер запущен
3. ✅ Cloudflared установлен
4. ✅ Туннель запущен
5. ⏳ Получить публичный URL

### Получить URL:
```bash
# Подождите 10-15 секунд после запуска tunnel
grep -oP 'https://[a-z0-9-]+\.trycloudflare\.com' /tmp/tunnel-output.txt
```

## 💡 Примечания

- Туннель **временный** и работает пока запущен процесс cloudflared
- URL меняется при каждом перезапуске
- Для постоянного URL нужен Cloudflare аккаунт
- Development сервер Flask не для production!

## 🔐 Безопасность

Текущая конфигурация:
- ✅ Публичный доступ только для чтения
- ✅ Нет доступа к генерации (нужны API ключи)
- ✅ Защита от скачивания произвольных файлов
- ✅ Health check endpoint

---

**Статус**: Веб-интерфейс запущен и готов! 🎉

Для получения публичного URL проверьте:
```bash
cat /tmp/tunnel-output.txt | grep trycloudflare
```
