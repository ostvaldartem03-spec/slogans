# 🎬 Cannes-Level Slogan Generator

Генеративно-аналитический пайплайн для создания 400 оригинальных рекламных слоганов уровня Cannes Lions на основе корпуса из ~15K существующих слоганов.

## ✨ Особенности

- **Генерация с LLM**: GPT-4/Claude с разнообразными креативными брифами
- **Проверка новизны**: Embedding-based + N-gram анализ против корпуса
- **Автоматическая фильтрация**: Безопасность, токсичность, клише
- **Качественный скоринг**: Punchiness, Wit, Clarity, Twist
- **Воспроизводимость**: Seed-based, версионированные зависимости
- **Мультиязычность**: Русский и английский

## 📊 Пайплайн

```
Корпус (15K) → Дедупликация → Эмбеддинги/N-граммы
                                    ↓
Генерация (2.5K) → Безопасность → Новизна → Внутренняя дедупликация
                                    ↓
              Скоринг качества → Ранжирование → Top-400
                                    ↓
                  JSONL / CSV / TXT + Отчёт
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Создать виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установить зависимости
pip install -r requirements.txt
```

### 2. Настройка API ключей

Создайте `.env` файл:

```bash
cp .env.example .env
```

Отредактируйте `.env` и добавьте свои API ключи:

```
OPENAI_API_KEY=sk-your-key-here
ANTHROPIC_API_KEY=sk-ant-your-key-here
TARGET_LANGUAGE=ru
SEED=42
```

### 3. Конфигурация (опционально)

Отредактируйте `config.yaml` для настройки параметров пайплайна:

- Язык генерации (`ru` / `en`)
- Размер пула кандидатов
- Пороги новизны и качества
- Веса для скоринга
- LLM модель и параметры

### 4. Запуск

```bash
cd src
python pipeline.py
```

Процесс займёт ~30-60 минут в зависимости от размера пула и API скорости.

## 📂 Структура проекта

```
slogans/
├── config.yaml              # Конфигурация пайплайна
├── requirements.txt         # Python зависимости
├── .env.example            # Шаблон для API ключей
├── README.md               # Этот файл
│
├── data/                   # Исходные данные (не в git)
│   ├── russian_slogans_numbered — копия.txt
│   └── English slogans
│
├── src/
│   ├── pipeline.py         # Главный пайплайн
│   └── modules/
│       ├── preprocessor.py     # Очистка и дедупликация
│       ├── embeddings.py       # Эмбеддинги и векторный поиск
│       ├── generator.py        # LLM генерация
│       └── scorer.py           # Скоринг качества и безопасность
│
├── out/                    # Результаты
│   ├── cannes_400.jsonl    # Полная структурированная выдача
│   ├── cannes_400.csv      # Табличный формат
│   └── cannes_400.txt      # Чистый текст (по одному на строку)
│
├── reports/
│   └── quality_report.md   # Отчёт о качестве и метриках
│
├── logs/
│   └── run.json            # Логи выполнения
│
└── .cache/                 # Кэш эмбеддингов и индексов
    ├── corpus_index_ru.faiss
    ├── corpus_index_ru.pkl
    └── raw_candidates.txt
```

## 📋 Выходные форматы

### JSONL (cannes_400.jsonl)

```json
{
  "id": "uuid",
  "text": "Ваш слоган здесь",
  "lang": "ru",
  "len_words": 4,
  "novelty": {
    "embed_sim_to_nn": 0.61
  },
  "style": {
    "punchiness": 0.82,
    "wit": 0.77,
    "twist": 0.65,
    "clarity": 0.9
  },
  "devices": ["антитеза", "аллитерация"],
  "flags": [],
  "total_score": 0.785
}
```

### CSV (cannes_400.csv)

Колонки: `id`, `text`, `lang`, `len_words`, `embed_sim`, `punchiness`, `wit`, `clarity`, `twist`, `total_score`

### TXT (cannes_400.txt)

Простой текстовый файл, по одному слогану на строку.

## ⚙️ Ключевые параметры

### Новизна

- **Embedding similarity threshold**: `0.80` - максимальная косинусная близость к корпусу
- **N-gram size**: `5` - размер непрерывных n-грамм для проверки
- **N-gram overlap**: `0.0` - нулевое пересечение обязательно

### Качество

- **Min punchiness**: `0.65` - лаконичность и ударность
- **Min wit**: `0.60` - остроумие, игра слов
- **Min clarity**: `0.70` - ясность и понятность
- **Min twist**: `0.50` - семантический поворот

### Генерация

- **Temperature**: `0.9` - высокая креативность
- **Top-p**: `0.95` - nucleus sampling
- **Presence penalty**: `0.7` - избегаем повторений

## 🎯 Критерии отбора

1. **Длина**: 2-8 слов (оптимально 4-6)
2. **Тон**: Дерзкий, умный, с неожиданным поворотом
3. **Запрещено**:
   - Названия брендов
   - Знаки ®™©
   - Клише ("лучший", "качество", "надёжность")
   - Медицинские/правовые обещания
   - Токсичность, дискриминация
   - Сравнения с конкурентами

## 🔬 Технологии

- **LLM**: OpenAI GPT-4 / Anthropic Claude
- **Embeddings**: sentence-transformers (multilingual MPNET)
- **Vector Search**: FAISS
- **NLP**: spaCy, NLTK, RapidFuzz
- **Data**: pandas, numpy
- **Export**: jsonlines, CSV

## 📈 Ожидаемые результаты

При запуске на полном корпусе (~15K слоганов):

- **Генерация**: ~2500 кандидатов
- **После фильтров**: ~600-800 прошедших все проверки
- **Финальная выдача**: Топ-400 по качеству

Распределение:
- Средняя длина: 4-6 слов
- Средний total_score: 0.75-0.85
- Novelty: < 0.80 к ближайшему в корпусе

## 🐛 Отладка

### Если мало кандидатов проходят фильтры:

1. Снизьте `embedding_similarity_threshold` в `config.yaml` (напр., до 0.75)
2. Увеличьте `candidate_pool_size` (напр., до 3500)
3. Снизьте пороги качества (`min_punchiness`, `min_wit`, и т.д.)

### Если генерация слишком медленная:

1. Уменьшите `candidate_pool_size`
2. Увеличьте `batch_size`
3. Используйте GPT-3.5-turbo вместо GPT-4

### Проверка промежуточных результатов:

- Сырые кандидаты: `.cache/raw_candidates.txt`
- Логи: `logs/run.json`
- Отчёт: `reports/quality_report.md`

## 📝 Лицензия

MIT

## 👥 Авторы

Создано с помощью Factory AI Droid

---

**Важно**: Для работы требуются API ключи OpenAI или Anthropic. Стоимость полного прогона ~$5-15 в зависимости от модели и размера пула.
